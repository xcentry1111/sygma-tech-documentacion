services:
  mkdocs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mkdocs_server
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    restart: unless-stopped

volumes:
  pip-cache:
